# =========================================================
# fastlane/.env.fastlane.prod 示例
# 用途：prod lane 的环境覆盖配置（bundle/scheme/export）
# 生效命令：bundle exec fastlane ios prod
# =========================================================

# 打包使用的 scheme（建议正式发布 scheme）
PROD_SCHEME={{SCHEME_DIS}}
# prod 对应的 Bundle ID
PROD_BUNDLE_ID={{BUNDLE_ID_DIS}}
# prod 对应的描述文件名称（manual signing 下需要）
PROD_PROFILE={{PROFILE_DIS}}
# 导出方式：通常 prod 用 app-store
PROD_EXPORT_METHOD=app-store
# 构建配置：通常 Release
PROD_CONFIGURATION=Release
# 是否上传到蒲公英：生产可设 false，避免重复分发
PROD_UPLOAD_PGYER=false
