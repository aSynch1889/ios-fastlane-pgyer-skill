# =========================================================
# fastlane/.env.fastlane.staging 示例
# 用途：staging lane 的环境覆盖配置（bundle/scheme/export）
# 生效命令：bundle exec fastlane ios staging
# =========================================================

# 打包使用的 scheme（默认建议和发布 scheme 保持一致）
STAGING_SCHEME={{SCHEME_DIS}}
# staging 对应的 Bundle ID
STAGING_BUNDLE_ID={{BUNDLE_ID_DIS}}
# staging 对应的描述文件名称（manual signing 下需要）
STAGING_PROFILE={{PROFILE_DIS}}
# 导出方式：development / ad-hoc / app-store / enterprise
STAGING_EXPORT_METHOD=ad-hoc
# 构建配置：Debug / Release
STAGING_CONFIGURATION=Release
# 是否上传到蒲公英：true / false
STAGING_UPLOAD_PGYER=true
